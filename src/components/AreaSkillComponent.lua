function AreaSkillComponent()
	local self = {}

	local onSelection = 0
	local radius = 50
	local range = 200

	local function query(entities, onHit)
		local entities = {}

		
		

		onHit(entities)
	end

	return self
end